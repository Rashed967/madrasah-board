
export const metadata = {
    title: 'জোন লিস্ট', 
    description: 'সকল জোন তালিকা',
  };


export default function AllZoneLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return <>{children}</>;
  }